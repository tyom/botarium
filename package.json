{
  "name": "botarium",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "cli": "bun run packages/core/bin/cli.ts",
    "dev": "bun run --filter botarium dev",
    "dev:electron": "bun run --filter @botarium/electron dev:electron",
    "build": "bun run --filter '*' build",
    "package": "bun run --filter @botarium/electron package",
    "test": "bun test apps packages",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "bun run typecheck && bun run format:check && bun run lint"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/bun": "^1.3.6",
    "concurrently": "^9.2.1",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-svelte": "^3.14.0",
    "globals": "^17.0.0",
    "prettier": "^3.8.0",
    "prettier-plugin-svelte": "^3.4.1",
    "typescript": "^5",
    "typescript-eslint": "^8.53.0"
  }
}
